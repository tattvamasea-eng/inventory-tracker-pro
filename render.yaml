services:
  - type: web
    name: inventory-tracker-pro
    env: python
    buildCommand: pip install streamlit pandas
    startCommand: streamlit run inventory_tracker.py --server.port $PORT --server.address 0.0.0.0
    envVars:
      - key: PYTHON_VERSION
        value: "3.9"